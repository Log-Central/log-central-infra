input {
  rabbitmq {
    host     => "rabbitmq"
    queue    => "logs_pipeline_2"
    durable  => true
    codec    => "json"
  }
}
output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-pipeline-2"
  }
  stdout { codec => rubydebug }
}